version: '3.8'

services:
  pulsestream:
    build: .
    container_name: pulsestream-downloader
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - PORT=8000
